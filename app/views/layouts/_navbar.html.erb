<nav class="navbar navbar-default navbar-static-top">
  <div class="container">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <%= link_to 'Easy Deposit', root_url, class: 'navbar-brand' %>
    </div>
    <div id="navbar" class="navbar-collapse collapse">
      <ul class="nav navbar-nav">
        <% if current_user? %>
          <%= navbar_link_to 'Deposit Items', new_deposit_url %>
        <% end %>
      </ul>
      <ul class="nav navbar-nav navbar-right">
        <% if current_user? %>
          <li class="dropdown">
            <%= link_to user_path, class: 'dropdown-toggle', data: { toggle: 'dropdown' }, role: 'button', 'aria-expanded' => false do %>
              <%= gravatar_tag current_user.email, size: 20 %> <span class="caret"></span>
            <% end %>
            <ul class="dropdown-menu" role="menu">
              <%= navbar_link_to 'Sign Out', destroy_user_session_path, method: :delete %>
            </ul>
          </li>
        <% else %>
          <%= navbar_link_to 'Sign In', new_user_session_path %>
        <% end %>
      </ul>
    </div><!--/.nav-collapse -->
  </div>
</nav>

